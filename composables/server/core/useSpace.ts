export default () => {
  // Export Function to be used
  //const FunctionName = () => {
  //    return new Promise(async (resolve, reject) => {
  //        try {
  //            //Do Something
  //            resolve();
  //        } catch (err) {
  //            reject(err);
  //        }

  //Return Function to be used
  return {
    //FunctionName,
  };
};
