{
    "rewrites": [
        {
            "source": "/auth/:path*",
            "has": [
                {
                    "type": "host",
                    "value": "*.enab.app"
                }
            ],
            "destination": "/auth/:path*"
        }
    ],

    "buildCommand": "yarn build",
    "devCommand": "yarn dev",
    "installCommand": "yarn install"
}